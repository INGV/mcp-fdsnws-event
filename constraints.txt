# Fully resolved dependency set for reproducible builds.
#
# Generated inside the image the project actually ships (python:3.11-slim), NOT on
# a developer machine: the resolution is interpreter- and platform-sensitive (numpy
# requires >=3.11, and wheel availability differs between amd64 and arm64), so a
# set frozen on another interpreter would not be installable here.
#
# Regenerate with:
#   docker run --rm -v "$(pwd)":/w -w /w python:3.11-slim bash -c '\
#     apt-get update -qq && apt-get install -y -qq gcc gfortran libblas-dev liblapack-dev && \
#     python -m venv /tmp/v && /tmp/v/bin/pip install -q --upgrade pip setuptools wheel && \
#     /tmp/v/bin/pip install -q ".[dev]" && /tmp/v/bin/pip freeze' \
#   | grep -v "^mcp-fdsnws-event-server" | sort -f > constraints.txt
#
# Used as `pip install -c constraints.txt` (see Dockerfile). Constraints pin the
# versions of whatever is installed without themselves forcing installation, so
# this file stays valid for both the runtime and the [dev] extra.
annotated-types==0.8.0
anyio==4.14.2
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.0
charset-normalizer==3.4.9
click==8.4.2
contourpy==1.3.3
cryptography==49.0.0
cycler==0.12.1
decorator==5.3.1
fonttools==4.63.0
greenlet==3.5.4
h11==0.16.0
httpcore==1.0.9
httpx-sse==0.4.3
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
jsonschema-specifications==2025.9.1
jsonschema==4.26.0
kiwisolver==1.5.0
lxml==6.1.1
matplotlib==3.11.1
mcp==1.29.0
numpy==2.4.6
obspy==1.5.0
packaging==26.2
pillow==12.3.0
pluggy==1.6.0
pycparser==3.0
pydantic-settings==2.14.2
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.20.0
PyJWT==2.13.0
pyparsing==3.3.2
pytest==9.1.1
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-multipart==0.0.32
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
scipy==1.17.1
six==1.17.0
SQLAlchemy==2.0.51
sse-starlette==3.4.6
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.0
